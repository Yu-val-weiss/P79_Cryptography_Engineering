[project]
name = "lab2"
version = "0.1.0"
description = "Authenticated key exchange"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
